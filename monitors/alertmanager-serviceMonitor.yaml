apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: alertmanager
spec:
  endpoints:
  - interval: 30s
    port: web
  selector:
    matchLabels:
      component: monitor
      owner: andrew
      app: alertmanager
